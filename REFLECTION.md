# REFLECTION

这次 Project A 让我理解了使用 coding agent和实现 coding agent harness并不是一回事。课程真正考察的是 agent 的运行机制：动作如何被严格解析、工具如何被安全调用、失败如何回灌下一轮、状态如何持久化、机制能否被测试证明。因此项目拆成 core、runtime、store、credentials、cli、web 模块，让各机制边界清楚、更易验证；这也让我在评审代码时能快速定位某一处改动属于哪个职责。我一开始更关注界面，想把它做成类似 VS Code 的工具，后来才意识到，真正的深度在 harness 内核：循环、护栏、反馈和记忆，而不是漂亮的外壳。

Superpowers 工作流对我很有帮助。brainstorming 让我先明确取舍，例如先完成可用 harness，再把 WebUI 改成对话式工作台；writing-plans 把宽泛目标落到具体文件、接口和测试上；冷启动验证暴露了 SPEC 中 T2/T3 依赖不清、parser shape validation 不完整、guardrail 优先级未固定等问题。若没有这些修订，实现很可能“看起来合理但边界不一致”。我还体会到 subagent 工作流的价值：把任务拆得足够小、接口写清楚，一个新鲜 subagent 才能独立推进而不跑偏；任务太大或依赖不清，它就会停下来反复问。不过部分技能也有形式大于实质的问题。不少决策是实现后才定型，真正拦住问题的往往是具体测试，而非技能清单本身。这说明技能只是脚手架，真正的工程判断仍要靠自己。

TDD 是约束 AI 的关键方法。先写失败测试一开始显得麻烦，却能防止 AI 直接给出“像是完成了”的实现。Basic Auth 要求未认证返回 401、.dockerignore 要求排除本地依赖与凭据、凭据测试要求密文不含 key 明文、WebUI 气泡测试暴露对齐问题，这些都让每个机制有可运行证据。红灯再绿灯的过程虽显麻烦，却是“先验证再宣称完成”最有效的落点，也让我习惯了把“完成”拆成可观测的证据。面对 AI 快速产出大量代码的场景，测试是唯一能让“做对了”变成可证实的判断；每次看到测试从红变绿，我才敢确认这一步真的做完了，而不是模型告诉我“已经完成”。

接入 DeepSeek 后，我明显感受到真实 LLM 和 mock 的差异。mock 适合确定性测试，DeepSeek 则可能出现 schema 不严格、JSON 后追加解释、重复查看文件、偶发 503。在开发过程中我发现，模型会编造已运行 npm test 并通过，但 timeline 里根本却没有实际执行；只靠提示词无法消除这类幻觉。后期通过靠 harness 的代码机制兜底，验证声明守卫拒绝未执行的验证声明，写后必须验证阻止反复重写同一文件，多 JSON 检测防止动作被静默丢弃，历史去重与有效迭代预算减少空转。每修一个问题，都让我更确信：与其在提示词里反复叮嘱模型要诚实、别重复，不如把约束写成确定的代码，让结果可被测试、可被重复。可靠 agent 需要状态机、反馈和边界检查共同支撑，机制必须落在代码里。

安全和部署也是重要收获。真实 API key 接入后，问题不只是能否调用，还包括 key 放哪、日志会不会泄漏、WebUI 公网部署后会不会被别人触发真实 run。项目使用 AES-256-GCM 加密凭据文件，通过 HARNESS_MASTER_PASSWORD 派生密钥，DEEPSEEK_API_KEY 只作 fallback；服务器部署可用 WEBUI_ADMIN_PASSWORD 开启 Basic Auth，Docker 分发补了 .dockerignore 和部署文档。凭据与分发这两条工程要求，迫使我想清楚了本地环境、服务器和镜像里分别能放什么、不能放什么。主密码只在运行时注入，真实 key 只进加密文件，镜像里一个都不能留。

WebUI 的变化体现了目标的变化。最早页面像运行控制面板，后来改成以对话为主体：文件、状态、最近运行和允许命令变成辅助面板，文件在右侧预览，工具调用与文件变更默认折叠。为了“聊天记录按时间正序”这类细节，也走了测试驱动。它还不是完整 IDE，但已能表达“对话驱动代码开发”的核心流程，也说明产品形态会随对问题的理解不断收敛。

如果重做，我会把“领域与机制设计”写得更早更细：先定 feedback 信号与护栏规则，再写 UI；也会更早接真实 LLM，而非先用 mock 打磨界面。对 Superpowers 的批判：它假设“先想清楚再做”，但很多边界只有接真实模型后才暴露；worktree 与 subagent 纪律适合大任务，单人小任务反而增加切换成本。

这次协作让我更清楚 AI 和人的分工。AI 能快速写代码、测试和文档，但方向选择仍需人决定：我要求 WebUI 默认 DeepSeek、提交信息中文化、部署考虑访问控制，也要求 REFLECTION.md 最终由我确认。当 AI 能完成大部分编码时，工程师的价值在于定义“做对”的标准、把标准写成可测试的机制，并守住人工确认。AI 辅助软件工程不是让 AI 一次性生成答案，而是通过 SPEC、PLAN、TDD、验证和人工确认，把 AI 的速度放进可审查、可复现、可交付的工程流程。
